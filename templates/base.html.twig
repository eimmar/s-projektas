<!DOCTYPE html>
<html>
    <head>

        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('build/css/main.css') }}">
        {% endblock %}
    </head>
    <body>
    <div class="container">
        {{ render(controller('App\\Controller\\IndexController::search', { 'request': app.request})) }}
        {% block body %}{% endblock %}
    </div>
        {% block javascripts %}
            <script src="{{ asset('build/js/main.js') }}"></script>
        {% endblock %}
    </body>
</html>
