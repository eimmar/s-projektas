{% extends 'base.html.twig' %}

{% block title %}FuelType{% endblock %}

{% block body %}
    <h1>FuelType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fuel_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fuel_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fuel_type_index') }}">back to list</a>

    <a href="{{ path('fuel_type_edit', {'id': fuel_type.id}) }}">edit</a>

    {{ include('fuel_type/_delete_form.html.twig') }}
{% endblock %}