{#{% form_theme form 'jquery.collection.html.twig' %}#}
{{ form_start(form) }}

{{ form_row(form.visitDate) }}

{{ form_row(form.vehicle) }}
<a href="{{ path('vehicle_new') }}">{{ 'vehicle.action.new'|trans }}</a>

<table>
    <tr>
        <th>Pavadinimas</th>
        <th>Aprasymas</th>
        <th>Kaina</th>
        <th>Trukme</th>
    </tr>
    {% for visitService in form.visitServices %}
        {{ include('visitService/_form.html.twig', {'form' : visitService}) }}
    {% endfor %}
</table>


{#<ul class="tags" data-prototype="{{ form_widget(form.visitServices.vars.prototype)|e('html_attr') }}">#}
        {#{% for visitService in form.visitServices %}#}
            {#<li>{{ form_widget(visitService) }}</li>#}
            {#{{ include('visitService/_form.html.twig', {'form' : visitService}) }}#}
        {#{% endfor %}#}
{#</ul>#}


{#{% for visitService in form.visitServices %}#}
    {#{{ form_widget(visitService) }}#}
    {#{{ include('visitService/_form.html.twig', {'form' : visitService}) }}#}
{#{% endfor %}#}
{#{{ form_row(form.visitServices) }}#}

<div>
    <br/>
    Apytiksle kaina {{ visit.totalInclTax }} EUR
</div>

<div>
    Numatoma darbu trukme {{ visit.totalDuration }} val.
</div>

{{ form_row(form.status) }}
{{ form_widget(form._token) }}

{#<button class="btn">{{ button_label|default('Save') }}</button>#}
{{ form_end(form) }}
