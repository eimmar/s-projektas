{% extends 'base.html.twig' %}

{% block title %}{{ 'visit.new'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'visit.new'|trans }}</h1>

    {{ include('visit/_form.html.twig') }}

    <a href="{{ path('visit_index') }}">{{ 'visit.action.index'|trans }}</a>
{% endblock %}